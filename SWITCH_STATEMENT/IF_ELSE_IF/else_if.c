#include <stdio.h>

int main()
{
    int number;
    printf("ENTER THE NUMBER =" );
    scanf("%d",&number);
    if(number==10)
    printf("NUMBER EQUAL TO %0d",number);
     else if(number==20)
    printf("NUMBER EQUAL TO %0d",number);
     else if(number==40)
    printf("NUMBER EQUAL TO %0d",number);
    else
     printf("NUMBER NOT EQUAL");
    return 0;
}
